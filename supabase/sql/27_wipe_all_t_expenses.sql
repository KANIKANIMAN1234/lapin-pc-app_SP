-- =============================================================================
-- t_expenses 全行の物理削除（開発用・リセット用）
-- =============================================================================
-- ■ 実行前に必ずバックアップを取ってください。取り消しできません。
-- ■ Supabase の SQL Editor で postgres 権限として実行してください。
--
-- 消えるもの:
--   - t_expenses の全行（論理削除・未削除の区別なし）
--
-- 副作用:
--   - 各行 DELETE 時に t_expenses_recalc_actual_cost が走り、案件の actual_cost が
--     予算＋残存経費から再計算されます（経費が空なら予算分のみ）。
--
-- 補足:
--   - receipt_image_url に保存したファイルがある場合、Storage 上のオブジェクトは
--     自動では消えません。必要なら Storage 側を別途整理してください。
-- =============================================================================

BEGIN;

DELETE FROM t_expenses;

COMMIT;
