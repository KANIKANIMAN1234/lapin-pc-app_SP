-- =============================================================================
-- t_reports 全行の物理削除（開発用・リセット用）
-- =============================================================================
-- ■ 実行前に必ずバックアップを取ってください。取り消しできません。
-- ■ Supabase の SQL Editor で postgres 権限として実行してください。
--
-- 消えるもの:
--   - t_reports の全行（論理削除・未削除の区別なし）
--
-- 補足:
--   - audio_url 等に外部・Storage の URL がある場合、ファイル本体は自動では消えません。
--     必要なら Storage 側を別途整理してください。
-- =============================================================================

BEGIN;

DELETE FROM t_reports;

COMMIT;
